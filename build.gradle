apply plugin: 'java'
apply plugin: 'war'
apply plugin: 'jetty'

httpPort = 8080
stopPort = 9090
stopKey = 'stopKey'

repositories {
  mavenCentral()
}

dependencies {
  providedCompile 'javax.servlet:servlet-api:2.5'
}
